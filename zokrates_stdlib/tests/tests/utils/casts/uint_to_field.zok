from "utils/cast" import cast

def main(u8 a, u16 b, u32 c, u64 d) -> (field, field, field, field):
    field e = cast(a)
    field f = cast(b)
    field g = cast(c)
    field h = cast(d)
    return e, f, g, h