from "utils/pack/bool/pack" import main as pack
from "utils/cast" import cast

// Pack u32[N] into one field element
def main<N>(u32[N] input) -> field:
	bool[N * 32] bits = cast(input)
    return pack(bits)
