#pragma curve bn128

from "utils/pack/bool/unpack128" import main as unpack
from "utils/cast" import cast

// Unpack a field element as 128 big-endian bits
// Precondition: the input is smaller or equal to `2**128 - 1`
def main(field i) -> u32[4]:
    u32[4] result = cast(unpack(i))
	return result