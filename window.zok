def sum<N>(field[N] a) -> field:
	field res = 0

	for u32 i in 0..N do
		res = res + a[i]
	endfor
	return res

def main(field[42] a) -> field:
	field res = 0

	field b = a[43]

	for u32 i in 0..(42 - 3) do
		res = res + sum(a[i..i+1])
	endfor

	return res