import "ecc/babyJubJubParams.code" as context
// Source: zCash protcol A.3.3.4
def main(field[2] pt, field[10] context) -> (field[2]):

	context = context()
	field A = context[8]
	field B = context[9]

	field x = pt[0]
    field y = pt[1]

	field lambda = (3*x*x + 2*A*x + 1) / (2*B*y) 
	field xOut = B*lambda*lambda - A - 2*x 
	field yOut = lambda * (x - xOut) - y 

    return [xOut, yOut]