import "EMBED/u32_to_bits" as to_bits
import "EMBED/u32_from_bits" as from_bits

def right_rotate(u32 e, field by) -> (u32):
	bool[32] b = to_bits(e)
	//u32 remainder = e - (shifted << by)
	return from_bits([...b[by..], ...b[..by]])

def main(u32 a) -> (u32):
	return right_rotate(right_rotate(right_rotate(right_rotate(right_rotate(a, 12), 12), 12), 12), 12)
