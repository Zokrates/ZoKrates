import "utils/multiplexer/2bit.code" as multiplex
def main() -> (field[2]):
	field bit = 1 
	
	field[2] output = [0, 0]

	field[2] a  = [0, 1]
	field[2] b = [1, 0]

	output == multiplex(bit, a, b)
	
	return output 