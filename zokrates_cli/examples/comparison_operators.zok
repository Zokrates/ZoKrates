// comparison operator example
def main(field x) -> field {
    field y = x >= 3 ? 1 : 5;
    field z = y <= x ? x**3 : y**3;
    y = x < 3 ? 2 : 6;
    z = y > z ? 1 : 2;
    x = x == x ? x : y;
    return x + y + z;
}