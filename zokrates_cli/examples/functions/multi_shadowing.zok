def add(field a,field b) -> field:
  field v = a + b
  return v

def sub(field a, field b) -> field:
  return a-b

def main(field a, field b) -> field:
  field c = add(a,b)
  field d = sub(a,b)
  return 0
